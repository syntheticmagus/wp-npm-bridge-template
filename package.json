{
  "name": "wp-npm-bridge-template",
  "version": "1.0.0",
  "description": "Template for bridging NPM-based functionality into a WordPress plugin.",
  "scripts": {
    "build": "webpack --config webpack.build.config.js && npm-build-zip --source=wp/wp-npm-bridge-template --destination=build"
  },
  "author": "syntheticmagus",
  "license": "ISC",
  "devDependencies": {
    "npm-build-zip": "^1.0.3",
    "webpack": "^5.68.0",
    "webpack-cli": "^4.9.2"
  }
}
